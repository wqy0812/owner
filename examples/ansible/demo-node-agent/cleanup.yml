---
- name: Remove demo node agent files
  hosts: demo_nodes
  gather_facts: false
  vars:
    agent_root: /tmp/newplatform-demo-agent
  tasks:
    - name: Remove isolated agent directory
      ansible.builtin.file:
        path: "{{ agent_root }}"
        state: absent
