---
# This playbook deploys the whole application stack in this site.

- name: install and configure k8snode
  remote_user: waspaas
  hosts: k8s_cert_controller
  connection: local
  gather_facts: false

  pre_tasks:
    - import_tasks: preflight_1175.yml
  roles:
    - k8s1175_cert
