---
- name: Write the Kubernetes encryption configuration
  ansible.builtin.template:
    src: encryption-config.yaml.j2
    dest: /approot1/paas/kube/cert/encryption-config.yaml
    owner: kube
    group: users
    mode: '0600'
  no_log: true

