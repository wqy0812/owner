---
#

- name: stop etcd
  tags:
    - install
    - update
  systemd: name=etcd state=stopped
  
- name: wait for etcd stopping
  tags:
    - install
    - update
  wait_for: timeout=10
  
- name: start etcd
  tags:
    - install
    - update
  systemd: name=etcd state=started
  
