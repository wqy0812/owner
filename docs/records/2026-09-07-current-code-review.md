# 当前工作区代码审核

审核范围为 `codex/platform-demo` 分支相对 `d65ab16` 的全部未提交改动，包含新增文件。审核后修复一项已复现问题，当前未发现阻止本地提交的缺陷。

## 已审核行为

- 首版统一使用当前数据库、Catalog 和原生作业合同，删除旧字段、旧摘要算法、全局参数存储和历史转换入口。资源范围声明从接口、持久化、摘要、规划、执行和 UI 整体移除；Owner 的 YAML 负责业务检查与文件操作。
- 集群重置按来源 Run 的安装主机组及当前成员锁定目标，保护 File Station 和镜像仓库，拒绝混合目标、不完整来源、多层基线和越界委派。执行前再次验证目标和安装记录，续跑保留未完成后检查的恢复身份。
- 空组件删除在数据库写锁内复核 Owner、所有版本和引用，删除及审计同事务提交。目录删除锁定工作区树摘要，使用持久化恢复记录补偿文件与 SQLite 的提交失败；动作入口保持保护。
- 前端新增目录树、目录操作、空组件删除和文件选择控件；新建场景或组件分支须显式确定适配维度。

## 本次修复

平台选项目录首次请求失败后，原创建表单把空目录视为没有必选维度，允许确认固定适配范围。现在保留目录错误状态，场景和组件分支创建均保持阻断，并提供重新加载入口；成功读取后恢复选择。取消的过期请求不更新目录或错误状态。

两项回归测试在修复前均因确认框错误启用而失败，修复后通过。浏览器注入目录 HTTP 503，同样验证了阻断及重试恢复。

## 当前验证

| 检查 | 结果 |
| --- | --- |
| `go test ./...` | 通过 |
| `go test -race ./...` | 通过 |
| `go vet ./...`、改动 Go 文件格式检查 | 通过 |
| `pnpm --dir web test` | 26 个文件、273 项测试通过 |
| `pnpm --dir web build` | 类型检查、生产构建通过 |
| `make check-docs test-deploy-script test-fixture-boundary` | 通过 |
| `make test-role-job ANSIBLE_PLAYBOOK=/tmp/owner-yaml-ansible/bin/ansible-playbook` | 20 个顶层测试通过；专门测试 Ansible 2.8.8 调度的 1 项按条件跳过。本次使用 ansible-core 2.18.6 |
| `make test-e2e-live` | 隔离 SQLite 与真实本地 Go API 的 4 项浏览器测试通过 |
| `git diff --check` | 通过 |

人工浏览器验证使用独立临时数据库及本地 API/Vite，视口为 1280 × 900。验证目录创建、文件写入、整目录删除与动作入口删除禁用；查看截图并测量页面宽度 1280/1280、弹窗宽度 978/978，无横向溢出。截图保存在忽略的 `output/playwright/review-*.png`。

本记录只证明本地代码审核与隔离回归，不代表推送、测试服务器部署或真实集群验收。
