# 88.55 Run 快照迁移与部署记录

2026-09-08，按用户“更新版本到88.55”和“允许停机迁移”的指令，将当前工作区部署至 `192.168.88.55`，保留原有业务数据。服务已恢复，地址为 <http://192.168.88.55:8080/>，页面版本为 `mtseb7od-fa90576d`。

## 发布身份与门禁

- Git 基点：`527e907`；包含当前未提交改动，未执行提交或推送。
- 发布源码 SHA-256：`55a9cdc591ef3c0d5c124da4c5a572353c3980cbe49aaa796531dc188835c958`。本记录及实施记录的部署状态更新写于发布后，属于后补文档。
- `make test-deploy` 的 local、runtime 阶段全部通过，`sourceUnchanged=true`，记录位于 `output/deployment-gate/20260908-160548-c6850960/`。
- 门禁包含全量 Go、前端覆盖率、12 个 live API 页面流程，以及固定 Ubuntu 18.04 / Ansible 2.8.8 / Python 3.6.9 容器的原生作业、续跑、回滚和 smoke 测试。
- 激活前再次核对源码摘要，调用 `./scripts/deploy-test-88-55.sh --skip-tests --disable-catalog-backup --verified-run-migration /var/lib/clusterforge/run-migrations/20260908T080658Z`。`--skip-tests` 复用刚完成且源码一致的统一门禁，构建、部署锁、源库绑定、备份、目标合同和制品校验照常执行。

## 迁移与切换

先在在线一致性备份副本完成真实数据转换预检，再确认活动 Run 和镜像构建为零。停服后检查数据库文件无进程持有，依次执行预检、绑定源摘要的正式迁移和 `verify-run-migration`，目标状态为 `ready`。原库只读，未走业务重建或数据清理流程。

| 项目 | 结果 |
| --- | --- |
| 源合同 | `clusterforge-v1-20260907-no-resource-contract` |
| 目标合同 | `clusterforge-v1-20260908-typed-run-snapshot` |
| 停服后记录时间 | 2026-09-08 16:16:01 +08:00 |
| 新服务进入 active | 2026-09-08 16:16:37 +08:00 |
| 源摘要 | `86824c367f8db9839340ce316e44267acdd49ab1972ac46de63e1a574ad66da1` |
| 独立源备份 SHA-256 | `c358b1ed8e2a44a8c8f577d6a4c367ab943464410abfab680ff46e7da40ce3ec` |
| 激活前目标库 SHA-256 | `c92a00b324055b9b3100258ea729492cafecf75c181a6f512d5dea6e862fd30c` |

原程序和数据库备份位于远端 `/var/lib/clusterforge/deploy-backups/20260908T081636Z-28381`。独立源备份 `target.db.source.db`、转换目标 `target.db`、迁移报告 `ready.json` 位于远端 `/var/lib/clusterforge/run-migrations/20260908T080658Z/`，与部署备份分开保留。既有目录备份功能的关闭状态保持不变。

新程序已开始运行并开放写入，此后不自动用旧库回退；切换过程没有触发回退。切换前恢复及写入后的保护边界已由部署脚本沙箱门禁覆盖，未在真实业务库执行回退演练。

## 独立验收

恢复后再次通过备份 CLI 创建一致性历史快照并核验，以仓库使用的 Go SQLite 驱动比较源备份和恢复后快照中的业务行。比较包含 rowid、Run 身份/状态/时间/续跑关系，以及相关表完整内容摘要。

| 保留内容 | 数量 | 结果 |
| --- | ---: | --- |
| Run | 33 | 标量身份与状态相同；31 成功、2 失败；全部详情 API 可读取 |
| 原作业包 | 33 | 数据库内容不变；逐一下载后 SHA-256 与原库一致，gzip/tar 可读 |
| 步骤 / 日志 | 138 / 14,188 | 行内容摘要一致 |
| 执行回执 | 45 | 行内容摘要一致 |
| 组件安装 / 场景安装 | 15 / 1 | 包含 Generation、备份及基线的完整行摘要一致 |
| 组件 / Release / 发布线 | 15 / 15 / 15 | 行内容摘要一致 |
| 场景 / Revision | 1 / 1 | 行内容摘要一致 |
| 环境 / 环境 Revision | 2 / 4 | 行内容摘要一致 |
| 归档文件 | 0 | 源数据无归档文件；本次无实际归档文件迁移样本 |

服务为 `active/running`，外部 HTTP 200，数据库合同、完整性及外键检查通过。工作台、组件、场景、环境列表及全部 33 条 Run 详情读取通过，当前无活动任务。45 个 HTML、版本和资源文件的外部响应字节与本地构建相同。新启动日志无错误。

| 制品 | SHA-256 |
| --- | --- |
| 平台程序 | `58339d0ee7eacaf47a4591079d4e696fa760fc497d92907af403b59e16738ea4` |
| 备份 / 迁移 CLI | `de429bce33856dbaf4ab5bc02cbd678eb31345848be97907da069f7656fef4a3` |
| Native Job CLI | `0a7583498ff2641d9e1a1fc87d7f137ccfba065e398dd376725d7ed0397da2e2` |

远端根分区检查时剩余 921 MiB（使用率 96%）；未删除既有备份。本次仅进行迁移、部署和读取验收，没有新增业务 Run 或执行真实环境 Ansible 作业。

本地脱敏验证证据位于 `output/deployment-88-55-20260908/`，包含门禁摘要、迁移摘要、脚本日志、业务行摘要、页面资源哈希和逐作业下载哈希。原始数据库保留在受限临时目录及上述远端备份目录，未加入仓库。
